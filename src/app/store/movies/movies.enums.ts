export enum ECondition {
    pinned = 'pinned',
    watched = 'watched',
}

export enum EActionsMovies {
    addPin = 'MOVIE pinned',
    addWatch = 'MOVIE watched',
    remove = 'MOVIE remove',
    set = 'MOVIE set',
}